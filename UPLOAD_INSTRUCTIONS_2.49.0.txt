v2.49.0 SIGNAL / JOURNAL STABILITY

Recommended: upload the CUMULATIVE ZIP contents to the GitHub repo, replacing matching files.
Do not delete/change Railway persistent volume or secrets.

After deploy, sidebar version should show:
2.49.0_SIGNAL_JOURNAL_STABILITY

Keep these Railway settings/secrets unchanged:
- RAILWAY_VOLUME_MOUNT_PATH / persistent volume
- DAY_MEMORY_ENABLED=1
- DHAN_CLIENT_ID
- DHAN_ACCESS_TOKEN
- LIVE_API_KEY
- Telegram variables (if already configured)

Live verification checklist
1. App/token must be active during market hours.
2. Auto Snapshot running at normal interval.
3. Watch a READY / BREAK TRIGGER setup: exact trigger must remain fixed until broken or invalidated.
4. After completed 3m trigger break, confirm barrier becomes BROKEN instead of moving to a new support/resistance and staying WAIT.
5. Auto Shadow Journal card should show Live decisions > 0 after live snapshots.
6. Railway journal caption should show LIVE APP DECISIONS RECORDED and persistent row count.
7. Download Decision Journal CSV after several live minutes and verify rows survive app refresh/restart.
