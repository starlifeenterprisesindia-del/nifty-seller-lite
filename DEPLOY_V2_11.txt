V2.11 UPDATE-ONLY DEPLOY — REPLACE / UPDATE, DO NOT DELETE THE APP

Expected version after update:
2.11.0_MAIN_AI_CONSOLIDATED

1. Keep the existing GitHub repository / Streamlit app.
2. Upload files listed in V2_11_UPDATE_LIST.txt to the same paths.
3. Replace/overwrite matching files; add services/summary_presenter.py and the new V2.11 docs/tests.
4. Do NOT delete the repository, data/ directory, Streamlit secrets, or saved JSON intentionally.
5. Redeploy/reboot Streamlit.
6. Top of app should show:
   - Main AI — Market View
   - Live Barrier + Range Map
   - All Features — Compact Evidence
   - Next 5–15 Min Market Outlook
   - Main AI + Full Audit PDF
7. Standalone Pre-Touch, duplicate Final Brain, duplicate Best CE/PE table and Developer Raw
   Data should not appear as normal top-level sections.
8. FII/DII journal should show Save / update selected date and no Delete selected date button.

Optional maintenance/debug (normally OFF):
- NSL_SHOW_MAINTENANCE=1 exposes cache/history reset controls.
- NSL_SHOW_DEVELOPER_DATA=1 exposes Developer Raw Market Data.

Important persistence note:
Streamlit local runtime JSON survives normal page refreshes while that deployment filesystem
lives, but a platform rebuild/redeploy can reset local storage. Keep the 15-session JSON
backup and use Restore journal backup after a reset. External durable storage would be needed
for guaranteed cross-redeploy persistence.
