Nifty Seller Lite v2.25.0 — Big Player Activity

- Added one responsive Big Player Activity screen for phone and laptop.
- Uses the same authoritative One-Brain snapshot: time-normalized NIFTY futures
  volume, futures price/OI setup, ATM option flow, Top-7 and barrier reaction.
- Shows BUYING / SELLING / MIXED, 0–100 activity, 2/3 persistence, reversal risk,
  time window, futures setup and compact reasons.
- Colour severity becomes stronger as participation rises; extreme selling is red,
  extreme buying is dark green and conflicts/absorption remain clearly labelled.
- Stores only a bounded same-day direction/score journal. It does not identify a
  specific institution, place orders or create a second decision brain.
- Confirmed 2/3 activity has a maximum six-point bounded adjustment inside the
  existing calculate_final_decision function.

v2.25.1 hotfix
- NORMAL activity no longer displays a misleading CONFIRMED 2/3 label.
- Persistence counts only WATCH-or-higher snapshots (score 40+).
- Futures OI now reads the canonical `open_interest` candle field returned by the
  existing Dhan candle parser; legacy `oi` remains a safe fallback.

v2.25.2 refinement
- Separates fresh LONG/SHORT BUILD-UP from SHORT COVERING/LONG UNWINDING.
- Closing-position flows use amber caution instead of fresh-buy/fresh-sell colours.
- Plain Hinglish explains whether new players entered or old positions were closed,
  plus the exact next confirmation to watch.
- Short covering/long unwinding receive only half the normal bounded One-Brain
  adjustment because they are weaker evidence of fresh directional commitment.

v2.26.0 alerts
- Separate compact Heavy Activity + Manual Price Alerts expander below the premium calculator.
- Ring/voice alert only for READY Very Strong/Extreme activity at score 75+ with 2/3 confirmation.
- Event latch prevents repeat sound on every 30-second snapshot and rearms after activity cools.
- One-shot manual BUY/SELL-labelled NIFTY target alert detects crossing from either side.
- Alerts are screen/session utilities only; they do not place orders or change One-Brain scores.

v2.26.1 CE/PE premium alert correction
- Automatic Heavy Alert is now explicitly labelled as overall NIFTY-market buying/selling,
  not a CE/PE trade call.
- Manual alert now selects CE/PE, BUY/SELL label and a live option-chain strike.
- The trigger is the selected contract's option premium, not the NIFTY spot level.
- Current premium, armed target and last triggered premium are shown clearly.

v2.27.0 two-stage sudden-activity alerts
- Adds an EARLY WARNING at score 65+ and 1/3 persistence when futures volume is
  elevated or options/Top-7 supports the same direction.
- Keeps the louder confirmed Heavy Alert at score 75+ and 2/3 persistence.
- Early and confirmed alerts have separate event latches, so an early move can
  escalate once without ringing on every 30-second snapshot.
- Voice warning includes fresh build-up/covering/unwinding, nearest-level reaction
  and asks for the next confirmation instead of presenting an early trade signal.
