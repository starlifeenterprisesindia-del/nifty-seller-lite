V2.10 UPDATE-ONLY DEPLOY — REPLACE, DO NOT DELETE

Expected version after update:
2.10.0_BARRIER_ROADMAP_SPEED_VIX

1. Keep the existing repository/app.
2. Upload the files listed in V2_10_UPDATE_LIST.txt to the same paths.
3. Choose Replace/Overwrite for matching files.
4. Do NOT delete the data/ folder, market_context JSON, option_state JSON or discipline state.
5. Redeploy/reboot Streamlit.
6. Open the app and confirm the top "Live Barrier + Range Map" appears.
7. During live market hours press Fetch Fresh Snapshot several times. Market Speed can use
   1m/3m/5m history immediately as it accumulates; VIX 5m/15m shows warming until history exists.

Important:
- Barrier Strength and Break Pressure are evidence scores, not guaranteed probabilities.
- India VIX is volatility/risk context, not direction.
- No order placement was added. The canonical Final One-Brain Decision remains unchanged.
