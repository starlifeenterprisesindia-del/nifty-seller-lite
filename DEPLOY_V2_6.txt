NIFTY SELLER LITE — V2.6 COMBINED MARKET MEMORY + FULL LIVE AUDIT PDF

IMPORTANT
- The user had not uploaded V2.5.
- This one V2.6 patch already contains every required V2.5 change plus the PDF-report changes.
- Upload only the 23 files listed in V2_6_UPLOAD_LIST.txt.
- Do not upload the full old app again.

DEPLOYMENT
1. Download and extract the V2.6 ZIP.
2. Open the existing nifty-seller-lite GitHub repository.
3. Choose Add file -> Upload files.
4. Upload the CONTENTS of the extracted patch folder into the repository root.
5. Existing matching files must be replaced; keep the folder structure for analysis/, services/, tests/ and ui/.
6. Commit message:
   V2.6 Market Memory and Full Live Audit PDF
7. Wait for Streamlit automatic redeployment. The new reportlab dependency will be installed from requirements.txt.
8. Do not recreate the Streamlit app and do not edit Secrets.
9. Confirm the version shown in the sidebar:
   2.6.0_MARKET_MEMORY_FULL_AUDIT_PDF
10. Fetch a fresh snapshot.
11. Click Generate Full Live Audit PDF and then Download Full Live Audit PDF.
12. After the commit is stable, delete obsolete release documents listed in V2_6_DELETE_LIST.txt if they are present.

LIVE TESTING
- Generate a PDF at the baseline signal.
- Generate another PDF around +5 minutes and +15 minutes.
- Compare snapshot ID, timestamp, NIFTY price, signal state, outlook paths, fake-move risk and invalidation.
- The PDF contains a verification worksheet and exact current audit tables.

SAFETY
- PDF generation reads the existing MarketSnapshot only.
- It does not call DhanHQ, state stores or calculate_final_decision.
- Exactly one strategy brain remains analysis/decision.py.
- App remains read-only and never places or exits broker orders.
