V2.10.1 BARRIER MAP RENDER HOTFIX — UPDATE ONLY

Replace/update these files in the existing repository. Do not delete the app:
- ui/components.py
- config.py
- app.py
- V2_10_1_HOTFIX.txt (new notes file)

Then commit/redeploy/reboot once.
Expected version: 2.10.1_BARRIER_MAP_RENDER_HOTFIX

This hotfix only fixes raw HTML tags appearing in the Live Barrier + Range Map.
No market calculations or One-Brain logic changed.
